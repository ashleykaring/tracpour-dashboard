// Placeholder for future Supabase wiring.
// Keep backend clients isolated here so src/lib/api.ts can switch from mock
// arrays to database queries without changing screen components.
export const supabase = null;
